import { StyleSheet } from "react-native";


const imageStyles = StyleSheet.create({
  image: {
    resizeMode: 'contain',
    width: 200,
    height: 60,
    marginTop: -10, 
  },
});

export { imageStyles };
